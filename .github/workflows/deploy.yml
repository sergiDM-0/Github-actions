name: Deploy to server 

on: push

jobs:
    first_jobs:
        runs-on: ubuntu-latest

        steps:
            - name: Hola mundo
              run: echo "Hola mundo"

    
    second_job:
        needs: first_jobs
        runs-on: ubuntu-latest

        steps:
            - name: hola gente 
              run: echo "Hola sergio"