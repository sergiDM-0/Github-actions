name: Deploy to server 

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest

        env:
            name : Sergio
        steps:
            - name: hola gente 
              run: echo "Hola $Sergio"

    
    second_job :

        if: ${{ github.ref == 'refs/heads/main'}}
        needs: first_job
        runs-on: ubuntu-latest

        steps:
            - name: hola gente 
              run: echo "Hola sergio"
    
